# test dicom files
can be found [here](https://saga-it.com/dicom/samples)

# dependencies
- `matplotlib` is not necessary to get a VDB, but is an optional sanity check.
  - if it is causing issues, just remove everything under `if plot` in `dicom_to_numpy/slices_to_np`
