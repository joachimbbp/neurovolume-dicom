[project]
name = "neurovolume-dicom"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "neurovolume>=0.1.0a20",
    "numpy==1.26.4",
    "pydicom>=3.0.2",
]
